#include <WiFi.h>

#define OnboardLED 2  // Onboard LED (GPIO2 on ESP32/ESP8266)

WiFiServer server(80);

void setup() {
  Serial.begin(9600);

  WiFi.mode(WIFI_STA);
  WiFi.begin("PriyomPhone", "1234priyom");

  Serial.print("Connecting to WiFi");
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.print(".");
    Serial.println(WiFi.status()); // Print actual status code
  }

  Serial.println("\nConnected!");
  Serial.print("IP Address: ");
  Serial.println(WiFi.localIP());

  server.begin();
  pinMode(OnboardLED, OUTPUT);
  digitalWrite(OnboardLED, HIGH); // LED OFF initially (active LOW)
}

void loop() {
  WiFiClient client = server.available();
  if (client) {
    String request = client.readStringUntil('\r');
    client.flush();
    Serial.println(request);

    if (request.indexOf("/ledon") != -1) {
      digitalWrite(OnboardLED, LOW);  // LED ON (active LOW)
    }
    if (request.indexOf("/ledoff") != -1) {
      digitalWrite(OnboardLED, HIGH); // LED OFF
    }

    // Send simple webpage back
    client.println("HTTP/1.1 200 OK");
    client.println("Content-Type: text/html");
    client.println("");
    client.println("<!DOCTYPE HTML>");
    client.println("<html>");
    client.println("<h1>ESP LED Control</h1>");
    client.println("<p><a href=\"/ledon\">Turn LED ON</a></p>");
    client.println("<p><a href=\"/ledoff\">Turn LED OFF</a></p>");
    client.println("</html>");
  }
}